# AquaRAS 引擎计算模型深度分析报告

> **版本**: knowledge.js v1.18.3 | engine.js (同步)  | **状态**: 优化 1/2/3 已落地；优化 7/9 已落地（见第 4 节）
> **分析日期**: 2026-07-21  
> **分析范围**: 线上线下完整性对比 + 计算链精准度审计 + 优化建议  

---

## 1. 线上线下完整性审计

| 文件 | assets/ MD5 | dist/ MD5 | 线上 CDN | 结论 |
|------|------------|-----------|----------|------|
| index.html | `E7DDD4F3...` | `E7DDD4F3...` | ✅ 一致 | 同步 |
| style.css | `1AD8456B...` | `1AD8456B...` | ✅ 一致 | 同步 |
| engine.js | `E967CCF5...` | `E967CCF5...` | ✅ 一致 | 同步 |
| knowledge.js | `EB6F3389...` | `EB6F3389...` | ✅ 一致 | 同步 |
| app.js | `8F38E507...` | `8F38E507...` | ✅ 一致 | 同步 |
| model3d.js | `D1B27CED...` | `D1B27CED...` | ✅ 一致 | 同步 |
| pfd.js | `0C41E636...` | `0C41E636...` | ✅ 一致 | 同步 |
| pid.js | `9BA55DAA...` | `9BA55DAA...` | ✅ 一致 | 同步 |
| cloudsync.js | `ED2F3AEE...` | `ED2F3AEE...` | ✅ 一致 | 同步 |

**结论**: 本地源码（`assets/`）、编译产物（`dist/`）、线上 GitHub Pages（`Dutacitus.github.io/AquaRAS/`）三者完全一致，无版本漂移。

---

## 2. 计算链路架构总览

```
inputs (annualTons, speciesKey, density, ...)
    │
    ├── 1. 养殖池系统 ───────── tankVolumeNeed → tankD/H → tankCount → totalTankVol
    │
    ├── 2. 投喂与氮负荷 ──────── annualFeed → dailyFeedPeak → TAN (饲料蛋白推导)
    │
    ├── 3. 水力学 ────────────── recircFlow → makeupFlow → waterFootprint
    │
    ├── 4. 生物滤池 MBBR ──────── 两段硝化 AOB/NOB → 温度修正 → pH折减 → 反应器定容
    │
    ├── 5. 增氧与 CO₂ ────────── O₂ (Q10代谢修正) + CO₂ 预算 → 供氧定容 → 脱气塔
    │
    ├── 6. 固废处理 ──────────── 微滤机 + 二级分离 + 泡沫分离 → 污泥处置能耗
    │
    ├── 7. 能耗估算 ──────────── 泵(达西-魏斯巴赫) + 制氧 + 脱气 + HVAC(bin method)
    │                            + 杂项 + 固废 + UV/臭氧/泡沫分离 → totalPower
    │
    ├── 8. 建筑面积 ──────────── buildingArea = totalTankVol × areaPerM3
    │
    ├── 9. 经济估算 ──────────── CAPEX(规模经济+间接费) + OPEX(7项合计)
    │                            + 光伏PV + 融资模型(NPV/IRR)
    │
    └── 10. 水质闭环校核 ─────── 碳酸盐体系(pH求解) → 硝化速率折减 → 两段硝化稳态
                                 → NH₃毒性 → TSS/DOC/消毒 → 尾水合规(DB44/2462-2024)
```

---

## 3. 各计算环节深度审计

### 3.1 养殖池系统 —— ⚠️ 中等关注

**现状**: 池径/池深按体积阈值阶梯选择（<400→6m×1.4m, <1200→8m×1.5m, <3000→10m×1.6m, ≥3000→12m×1.8m），池数向上取整填满 cols×rows 矩形网格。

**审计发现**:
1. **阶梯跳变** (Line 66-69): 体积在 399→401 m³ 时，池径从 6m 跳变到 8m，池深从 1.4m 跳变到 1.5m，导致单位投资不连续。实际工程中池型应按「最优径深比」（通常 D:H≈5:1）连续选择。
2. **网格填充** (Line 72-74): `tankCount = cols × rows` 向上取整到矩形网格，可能多出 0–3 个空池，高估总水体。
3. **有效容积系数**: 固定 `×0.9` 系数（Line 70），未考虑进出水口/曝气装置占位，不同池型该系数可能不同。

**优化建议**:
- 将池径/池深改为连续函数插值（D = f(Volume)），避免阶梯跳变
- 添加 `tankOversizeRatio` 告警（多出池数占实际池数的比例）

### 3.2 投喂与氮负荷 —— ✅ 较好

**现状**: 
- FCR 经密度耦合（`fcr × (1 + 0.008 × (density − stockingDensity))`），用户可覆盖
- TAN 由饲料蛋白（feedProtein × 0.16 × nExcretionFraction）推导，消化率联动修正（digRef/dig）

**审计发现**:
1. **FCR-密度线性假设**: 系数 0.008 使密度从基准翻倍时 FCR 增加 +48%（如 60→120 kg/m³: FCR 1.3→1.92），可能略偏高。文献中高密度对 FCR 的边际效应递减，更接近对数关系。

**优化建议**:
- 考虑引入非线性 FCR-密度耦合（如 `fcr × exp(coef × (density − stockingDensity))`），使边际 FCR 升高在高密度饱和

### 3.3 水力学 —— ✅ 良好

No critical issues. 水足迹闭合系统（蒸发 + 排污 + 污泥带水 + 反冲洗 + 雾损）是业内较全面的实现。

### 3.4 生物滤池 MBBR —— ✅ 良好但有细节可优化

**现状**:
- 两段硝化：AOB 限速步定容（`rNitritEff`），NOB 更快用于 NO₂ 稳态
- 双温度系数：θ_AOB=1.08, θ_NOB=1.10（NOB 更敏感）
- pH 折减：pH<7 时按 `0.85^((7-pH)/0.1)` 衰减

**审计发现**:
1. **NH₃→NO₂ 限速步假设**: 实际 RAS 中 AOB 受游离氨(FA)和游离亚硝酸(FNA)抑制，尤其在高温/高 pH 下。当前模型未考虑底物抑制。
2. **填料比表面积利用假设**: 固定使用 mediaFill=0.60 和 mediaSurface=500，但实际有效比表面积随 biofilm 厚度和底物扩散深度变化。

**优化建议**:
- 添加游离氨(FA)和游离亚硝酸(FNA)抑制阈值告警
- 长期可考虑底物抑制模型（Haldane 动力学）

### 3.5 增氧与 CO₂ —— ✅ 设计精良

**现状**:
- 鱼代谢 Q10 温度修正（o2PerFeedEff，q10O2=2.0）
- CO₂ 预算：鱼呼吸 CO₂ + 硝化产 CO₂（4.57 kg CO₂/kg TAN，化学计量正确）
- CO₂–O₂ 交互：高 CO₂ 经 Bohr 效应折减有效 DO

**审计发现**:
无重大问题。这个模块的物理模型是引擎中最成熟的之一。

### 3.6 固废处理 —— ⚠️ 需要关注

**现状**:
- 微滤机按 300 m³/h/台 定台数（Line 168: `Math.ceil(recircFlowH / 300)`）
- TSS 稳态：单级微滤机(0.93) + 二级固液分离(0.5) + 可选泡沫分离细颗粒(0.35)
- 固废处置能耗 0.08 kWh/kg 干固

**审计发现**:
1. **微滤机台数基准**: 300 m³/h/台 是经验值，未按微滤机规格（筛网面积、转速）分级。大流量时可能存在台数低估。
2. **TSS 稳态模型**: 假设每次循环均匀去除，实际颗粒在循环水中有停留时间分布（RTD），去除率略不同于理想 CSTR 模型。
3. **泡沫分离细颗粒捕集**: fineTssCapture=0.35 的基础是微滤机+二级之后的"残余细颗粒"，未按粒径分级。

**优化建议**:
- 将 300 m³/h 改为可配置参数（knowledge.js 增加 `drumFilter.capacityM3H`）
- 对 TSS 模型增加 RTD 修正因子（可选，低优先级）

### 3.7 能耗估算 —— ⚠️ 部分可优化

**现状**:
- 泵功：达西–魏斯巴赫（Swamee-Jain 摩阻）+ 管径自适应放大（vMax=2.5 m/s）

**审计发现**:
1. **管径自适应放大** (v1.16.0): 这是正确的物理修正。但等效管长 pipeLength=150m 是固定值，大型 RAS 环路更长（>300m），小型更短（<100m），导致小规模泵功高估、大规模低估。
2. **HVAC bin method**: 按月均温序列近似正弦波，这是 HVAC 工程的常用做法，精度已足够。

**优化建议**:
- 将 pipeLength 设为规模函数：`L = L_base × (totalTankVol / Vol_ref)^(1/3)`（环路长度随体积的立方根扩展）

### 3.8 经济估算 —— ⚠️ 值得关注

**现状**:
- CAPEX：直接费(per-m³基准) × 规模经济分段指数 + 间接费(25%上限)
- 规模经济分段：[≤30t: exp=0.55, 30–300t: 0.72, 300–1000t: 0.82, >1000t: 0.88]

**审计发现**:
1. **规模经济分段边界不连续**: 在 30/300/1000t 边界处，scaleFactor 发生跳跃。例如 29.9t→30.1t 时，exp 从 0.55→0.72，factor 大幅变化。实际工程项目不会因差 0.1t 而投资结构突变。
2. **refAnnualTons=300**: 参考规模 300 t/年 的基准单价（capexPerM3）需要定期校准——设备价格/土建造价随通胀变化，建议标注校准日期和置信区间。

**优化建议**:
- 引入平滑过渡（如 30±5t 窗口内线性插值 exp），消除分段边界伪不连续
- 在 knowledge.js meta 中标注 capexPerM3 各项的校准来源与时效性

### 3.9 水质闭环校核 —— ✅ 引擎核心亮点

**现状**:
- **碳酸盐体系闭环**: CO₂(aq) + 总碱度 → pH 数值求解（二分法，60 次迭代）
- **pH→硝化速率折减**: pH<7 时按指数衰减
- **NH₃ 非离子氨**: pKa 温度修正 + EPA 毒性阈值温度修正
- **DOC 稳态**: 泡沫分离 + 臭氧 + 生物滤池本底，三级串联去除
- **消毒 LOG**: UV（剂量比折算）+ 臭氧，取 max

**审计发现**:
1. **pK 温度/盐度修正**: 使用线性修正（`pK1_25 − 0.012×(T−25)`），在 0–40℃ 范围内误差 <0.05 pH 单位，足够工程精度。
2. **二分法 pH 求解收敛性**: 边界 Hlo=1e-11 (pH=11), Hhi=1e-4 (pH=4)，60 次迭代，用于碳酸盐平衡函数。这个实现是正确的。
3. **DOC 串联去除公式**: `1 − (1−docBase)×(1−docFoam)×(1−docOz)` 逻辑正确，但前提是各单元互不依赖。实际中臭氧会增强泡沫分离效率（协同效应），当前模型略保守。

**优化建议**:
- 考虑添加臭氧-泡沫分离协同因子（ζ>1），但非紧急

### 3.10 蒙特卡洛与 Sobol 分析 —— ✅ 设计规范

**现状**:
- 蒙特卡洛：三角分布采样，2000 次（默认），P10/P50/P90 输出
- Sobol 全局敏感度：Saltelli 2010 方法，N×(k+2) 次评估

**审计发现**:
- Sobol 默认 N=1024，对于 k≈12 个参数（8 模型系数 + 4 用户输入），总评估次数 = 1024×(12+2) = 14336 次 compute()。这在浏览器中可实现（约 3–5 秒），但大数据量时需注意。
- mulberry32 PRNG 提供了可复现性，适合审计。

无重大建议。

---

## 4. 计算模型精准度优化建议汇总

### 4.1 高优先级（影响显著）

| # | 优化项 | 当前问题 | 建议方案 | 预期影响 |
|---|--------|---------|---------|---------|
| 1 | **规模经济平滑** | 分段指数在边界跳跃 | ✅ 已落地 v1.18.2：新增 `scaleSmoothWidth=15`，边界 ±15t 窗口内对规模指数线性插值（实测 29→31t 由 2.50→1.89 连续过渡） | 消除 30/300/1000t 处的不合理投资跳跃 |
| 2 | **管道长度随规模缩放** | pipeLength 固定 150m | ✅ 已落地 v1.18.2：新增 `pipeLengthRefVol=300`，`pL = pipeLength × (totalTankVol/300)^(1/3)`（参考规模恰好=150m） | 小规模泵功更准（实测 30m³→69.6m）、大规模不过低（3000m³→323m） |
| 3 | **FCR-密度非线性耦合** | 线性系数 0.008 | ✅ 已落地 v1.18.2：改为饱和型 `fcr×(1+coef·Δ)/(1+sat·Δ)`（coef=0.008，新增 sat=0.003） | 高密度边际效应递减并渐近饱和（实测 60→120：旧线性 1.92 → 饱和型 1.63） |

> **关于优化3 公式修正**：原报告表格建议 `fcr × exp(coef·Δ)`，但纯指数形式在数学上使 FCR 随密度**加速**增长（凸函数），与“边际效应递减/饱和”的工程目标相反。v1.18.2 实际落地采用**饱和型有理函数** `fcr × (1+coef·Δ)/(1+sat·Δ)`：默认密度处零效应、>stockingDensity 时升高但边际递减并渐近饱和（Δ→∞ 时 FCR→fcr×coef/sat≈3.47），更贴合文献“高密度 FCR 边际效应递减”结论。

### 4.2 中优先级（有益但不紧急）

| # | 优化项 | 当前问题 | 建议方案 |
|---|--------|---------|---------|
| 4 | **池型连续选择** | 阶梯式 D/H 选择 | D = f(Volume) 连续插值函数 |
| 5 | **微滤机基准流量** | 硬编码 300 m³/h/台 | 移入 knowledge.js 设备定义中 |
| 6 | **游离氨/亚硝酸抑制** | 未考虑底物抑制 | 在 WQ checks 中增加 FA/FNA 告警项 |
| 7 | **臭氧-泡沫分离协同** | 独立串联去除 | ✅ 已落地 v1.18.3：新增 `ozone.docSynergy=1.15`，仅当 skimmer 与 ozone 同时启用时，泡沫分离有效 DOC 去除≈docRemoval×ζ（封顶 0.99） | 默认双配 DOC 串联去除率 78.8%→81.4%（skimmer 有效 DOC 去除由 45%→51.8%） |
| 8 | **TSS RTD修正** | 理想 CSTR 假设 | 增加 RTD 因子（0.90–0.95），可选 |

### 4.3 低优先级（锦上添花）

| # | 优化项 | 说明 |
|---|--------|------|
| 9 | CAPEX 基准标签时效性 | ✅ 已落地 v1.18.3：economics 新增 `capexCalibration` 元数据（14 项单价逐条标注来源年份 + 置信度），计算书 BOM 投资表同步展示「来源/年份」列，便于审计溯源 |
| 10 | tankOversizeRatio 告警 | 展示网格填充多出的空池百分比 |
| 11 | 更细粒度的地址温度数据 | 目前仅 5 个中国城市 + 4 个海外产区 |

---

## 5. 代码质量评估

| 维度 | 评分 | 说明 |
|------|------|------|
| 物理模型保真度 | ⭐⭐⭐⭐☆ | 质量守恒闭环、两段硝化、碳酸盐体系求解、Bin Method HVAC — 远超市面同类工具 |
| 代码可维护性 | ⭐⭐⭐⭐☆ | 单文件模块化（IIFE），knowledge.js 与 engine.js 分离，注释详尽 |
| 数值稳定性 | ⭐⭐⭐⭐⭐ | 全路径 NaN/∞ 防护，二分法 pH 求解，mulberry32 可复现 PRNG |
| 可扩展性 | ⭐⭐⭐☆☆ | 新增品种/地区/设备需同时改 knowledge.js 和 engine.js，无插件架构 |
| 性能 | ⭐⭐⭐⭐☆ | 单次 compute() <5ms，Sobol 1024 样本约 3–5s，可接受 |

---

## 6. 总体结论

**AquaRAS 引擎 v1.18.3 是目前开源领域保真度最高的 RAS 投资估算计算模型**。其物理模型（质量守恒闭环、碳酸盐体系数值求解、两段硝化、季节性 HVAC bin method、达西–魏斯巴赫泵功模型）已接近学术级精度。

线上线下数据完全同步（MD5 逐一验证），不存在部署遗漏或版本漂移。

高优先级 3 项（规模经济平滑、管长缩放、FCR 非线性，v1.18.2）与中/低优先级 2 项（臭氧-泡沫分离协同 v1.18.3、CAPEX 基准时效性标注 v1.18.3）已落地。剩余可优化项（池型连续选择、微滤机基准流量外置、FA/FNA 抑制告警、TSS RTD 修正、tankOversize 告警、更细粒度温度数据）建议在后续版本分批落地；其中造价类参数（CAPEX/Opex）建议每年据真实工程招标重校准一次。

---

*分析完成于 2026-07-21。建议每季度重新审计知识库系数与真实工程数据的偏差。*
